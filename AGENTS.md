# miniboos 项目约定

> CEO棍儿哥 × AI技术部 | 本档是 AI 开工前的进门须知，详细章程见《docs/协作机制.md》

## 项目地图

- 类BOSS直聘招聘系统，四个模块：
  - `backend/`：Spring Boot 3.4.5 + MyBatis + MySQL
  - `frontend-candidate/`：C端牛人 H5（Vue 3 + Vite + Vant 4）
  - `frontend-hr/`：B端HR H5（Vue 3 + Vite + Vant 4）
  - `frontend-admin/`：管理后台（Vue 3 + Vite + Element Plus）
- `docs/`：PRD.md、技术设计文档.md、UI设计规范.md、协作机制.md、ADR
- `scripts/dev-up.sh` 本地启动；`scripts/smoke-test.sh` 全链路冒烟测试（16环节）

## 开工流程（必读，不要等CEO交代）

1. **先判级**：按《docs/协作机制.md》变更三级制判断本次是 L1/L2/L3：
   - L1 微调（颜色/间距/文案）→ 直接改码，不动文档
   - L2 重构（页面重设计/行为改变）→ **先改文档 → CEO确认 → 再改码**
   - L3 新增（新功能模块/业务规则）→ **先出变更提案（PRD影响+表结构+工作量）→ CEO拍板 → 再动手**
2. **文档同步**：口说无凭，文档为证。改码必须同步对应文档（PRD/UI设计规范/技术设计文档）
3. **验收**：功能按 PRD 条目逐条点，UI 以《UI设计规范.md》为尺子，不达标要指出违反哪条

## 硬性纪律

- 改了 `backend/src` 下任何接口代码，收尾前必须跑 `scripts/smoke-test.sh` 并全绿才算完
- UI 实测数据优先于脑补（v4教训：主色青#00a6a7非绿，来自浏览器实测）；测绘参照物用浏览器工具取计算样式
- `.env` 存密钥：绝不读取内容、绝不入库（.gitignore 已覆盖）、绝不写进任何文档
- git 提交信息用中文，带决策上下文（谁拍板/为什么改），参照 git log 既有风格
- 跨四个模块的影响面排查（如"改状态机找所有受影响页面"）优先派子代理去扫，只带结论回来

## UI 参照物

- 当前参照：BOSS直聘（2026-09-16 拍板）；管理后台参照腾讯云控制台/Element官方demo——后台要效率不要个性
- CEO 指认新参照（含 APP 页面截图）统一存 `docs/ui-benchmark/`（命名：APP名-页面名.png）

## 交付验收制度（2026-09-16 CEO定稿：CEO不是测试员）

**质量内建：AI 认为可交付，才有资格交给 CEO。CEO 只做最终确认；CEO 发现本应由 AI 自查出的问题 = AI 失职。**

UI 改动交付门槛（五步缺一不可）：
1. **起服务亲眼看**：浏览器逐页打开改动页面并截图，AI 必须读图自查——只看代码不算验收
2. **并排对标**：与参照物同类页对照四维：间距密度、字号层级、色彩克制、组件质感
3. **过尺子**：逐条对照《docs/UI设计规范.md》验收口径，不达标要指出违反哪条
4. **报差距**：交付时附自查报告（截图+已修问题+已知差距清单）；声称"零差距"=没自查
5. **再交人**：自查全部通过才提交 CEO；CEO 只打钩/打叉，不做第一轮测试

## 沟通

- 称呼CEO为"棍儿哥"，默认用中文交流
